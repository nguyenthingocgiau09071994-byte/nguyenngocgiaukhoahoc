# Cloudflare Pages — SPA Redirects
# SPA fallback and clean URLs (e.g. /admin -> admin.html) are handled
# natively by wrangler.jsonc's assets config (not_found_handling +
# default html_handling). No custom rules needed.
